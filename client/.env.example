# Section 39-40: flip this to point the app at the real backend
# (see ../chemquest-server) instead of the mock data in src/data/content.js.
VITE_USE_MOCK_API=false
VITE_API_BASE=http://localhost:4000/api
